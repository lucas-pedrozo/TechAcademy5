import ContainerUser from "@/components/admin/ContainerUser";

function AdmUser() {
    return (
        <main className="py-10 px-2.5 max-w-[1220px] m-auto">

            <section className="overflow-hidden overflow-y-scroll max-h-[700px] || flex flex-col gap-2.5 w-full">
                <ContainerUser />
                <ContainerUser />
                <ContainerUser />
                <ContainerUser />
                <ContainerUser />
                <ContainerUser />
                <ContainerUser />
                <ContainerUser />
                <ContainerUser />
                <ContainerUser />
            </section>
        </main>
    );
}
export default AdmUser;